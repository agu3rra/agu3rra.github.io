# Virtual environment
This project uses UV for managing dependencies and a virtual environment. Before you can run any commands to see the page render locally, ensure you activate it with:

```bash
source .venv/bin/activate
```

Once the environment is active, you can run `python` and all required libraries should be available.